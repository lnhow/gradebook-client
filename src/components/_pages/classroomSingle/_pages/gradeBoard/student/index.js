import {
  Container,
  Box,
  Typography,
} from '@mui/material';

export default function StudentGrade() {
  return (
    <Container maxWidth='xl'>
      <Box marginY={2}>
        <Typography>Điểm của tôi</Typography>
      </Box>
    </Container>
  )
}
